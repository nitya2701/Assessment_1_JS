//Write a JavaScript program that accept two integers and display the larger.

function largrOfTwo(a,b)
{
    if(a>b)
    {
        console.log(`${a} : a is the larger of two `);
    }
    else{
        console.log(`${b} : b is the larger of two `);
    }
}
largrOfTwo(2,4);